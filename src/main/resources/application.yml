#dog food
config.server: http://localhost:8888/
server.port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sanjuthomas/config-store
          search-paths:
            - service-*
          default-label: master